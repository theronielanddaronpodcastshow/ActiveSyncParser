/**
 * <p>
 * This is the ActiveSync Parser. It parses ActiveSync logs, aggregating the log files, breaking them up by device,
 * and then ordering them (and eliminating duplicates).
 * </p>
 * 
 * @author DaRon
 */
module local.rdps.svja {
}
